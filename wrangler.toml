# Used by @cloudflare/vitest-pool-workers for runtime testing.
# Consumers should add these bindings to their own wrangler.toml.

name = "durable-bash"
main = "src/index.ts"
compatibility_date = "2025-03-01"

[[durable_objects.bindings]]
name = "FS"
class_name = "FsObject"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["FsObject"]
